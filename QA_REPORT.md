# 로컬 검증 기록

## 2026-09-09 고도화 회귀 검증

12:54 KST root 최종 검증: **단위 85개 + D1/런타임 통합 74개 + Chromium 21개 = 총 180개 통과**. 타입·린트·빌드, 설정 구조 검사, 평가 dry-run, 로컬 배포 후보 생성과 운영 SQL도 통과했다. strict production 검사는 미확정 외부 ID 때문에 의도적으로 종료 1을 반환한다. 독립 최종 범위별 리뷰는 **APPROVE**, 열린 지적 0개다. 근거는 `.omx/reviews/hardening-*`와 `.omx/verification-hardening.json`이다. 이번 작업에서는 실제 모델 호출·원격 배포·Git 게시를 수행하지 않았다. 이전 Gemini 실제 평가 8회는 아래 역사적 기록으로 남기며 새 계약의 품질 증거로 재사용하지 않는다.

| 범위 | 검증하는 실패 / 기대 | 근거 |
| --- | --- | --- |
| 편집·조회 경합 | 500/409에서 초안 유지와 포커스, 같은 의도의 요청 ID 유지, A→B→A 전환, 같은 기준의 늦은 pending 응답, 버전과 요청 순서 경합 | `tests/e2e/frontend-failures.spec.ts`의 제어된 API 응답 |
| 익명 유입 | 1,000회 목록/설정 조회에 owner/cookie 생성 없음, 첫 성공만 owner 저장, 마지막 전역 생성 슬롯 경합, 배포 환경/limiter 부재 거절 | `tests/integration/admission.test.ts` |
| 저장·삭제 | UTF-8 논리 바이트 집계, 원문·캐시 증가 실패 전체 롤백, 이력 제한, 삭제 후 일일 유입/비용 장부 보존 | 같은 D1 통합 테스트, 실제 SQLite 트리거 |
| 질문 답변 | 서버 소유 질문 문맥 저장, 오래된 답변/동시 답변 거절, 저장 실패 시 기존 질문 유지, 답변→Queue→새 제안→승인 | `tests/integration/answers.test.ts`; 공급자 HTTP는 가짜 응답 |
| 입력·AI 제안 | 큰 조합 입력/재시도를 비용 예약 전 차단, 숫자와 인용 불일치·모호한 숫자 거절, 정정 대상의 동일 이름 새 key 거절 | answers / engine / model 테스트 |
| 평가 정직성 | 구조가 유효해도 오답/보호 손실은 품질 실패 및 종료 1, 질문 기대는 명시된 사례만 허용 | `tests/unit/eval-live-cli.test.ts`; 합성 키의 별도 CLI 프로세스 |
| 운영 진단 | 오래된 pending/running, uncertain, 정책 위반, 저장 누락/집계 차이 critical; 진단 장애가 복구를 막지 않음 | `tests/integration/ops-health.test.ts` |
| 환경·후보 파일 | staging/production 분리, 동시성 1, 누락/공유/placeholder ID 탐지; 빈 번들·symlink 거절, 비밀 파일·소스맵 제외, SHA-256 일치 | deploy-readiness / release-manifest unit 테스트 |

회귀 순서 증거: 익명 저장 경계 4개 실패, 질문 답변 초기 3개 실패, 긴 입력 재시도 1개 실패, 숫자/identity 경계 실패, 운영 누락/집계 3개 실패를 확인한 뒤 수정했다. 최초 eval 종료 오류는 선행 분석의 오프라인 CLI에서 재현했으며 해당 새 회귀를 구현 전 다시 실행한 것은 아니다. 브라우저 경합의 추가 리뷰 결과와 수정 기록은 `.omx/reviews/hardening-frontend-*.md`에 남긴다. 강화한 저장/조회 경합 시험에서는 root가 과거 순서 판정을 일시 주입했을 때 새 내용 버전이 나타나지 않아 종료 1이 되는 것을 확인하고 원래 수정본을 복원했다. 이는 수정 후 결함 주입 검증이며 모든 새 시험을 첫 수정 전에 실행했다고 주장하지 않는다.

정적 자산 확인은 Vite가 아닌 실제 로컬 Workers ASSETS `8787/`에서 수행했다. CSP 등 헤더, 200 응답, 화면 렌더링, JS 오류 0개·CSP 위반 0개를 확인했다. 이 화면 검사에서는 API를 대역 처리했고 사용자 데이터를 읽지 않았다. `artifacts/hardening-static-assets-check.json`, `hardening-static-assets-desktop.png`가 근거다.

실제 로컬 Worker 익명 조회 1,000건을 동시 20개씩 전송한 제한된 점검에서도 오류·새 쿠키 0건, 소유자/공간/장부 행 수 증가 0건이었다. 관측 p95는 약 50ms이나 개발 장비의 이 조회 시나리오에만 해당하며 운영 처리량 증거로 사용하지 않는다. `artifacts/hardening-local-read-burst.json`에 집계가 남아 있다.

로컬 마이그레이션 0004/0005 전후의 작업 공간 1개·원문 3개·스냅샷 4개·비용 행 3개가 유지됐으며 저장량 카운터와 재집계 차이는 0이다. 운영 SQL도 새 테이블에서 실행했다. 행 수/집계 검사는 모든 기존 값의 바이트 동일성을 증명하는 검사가 아니다.

**미검증:** 새 숫자·질문 계약의 실제 Gemini 응답, 처음 보는 15~30개 원문, 공개 트래픽 성능, 원격 D1/Queue/cron/알림 수신, 실제 코드 롤백, 원격 CI와 배포. 숫자 스칼라 및 정규화 이름 충돌 검사는 제한된 보호이며 일반적인 의미 정확도·모든 별칭 우회 차단을 보장하지 않는다. 전체 모델 입력 축소·실행별 prompt/schema provenance·명령 응답 저장량 개선은 후속 범위다.

## 2026-09-09 Gemini 연결과 실제 평가

10:15 KST root `npm run verify`: **단위 61개 + D1/런타임 통합 47개**, 타입·린트·빌드 통과. 10:08 KST 격리된 Chromium **9개** 통과. 총 자동 검증 **117개**이며 실제 AI 평가 8회와 로컬 전체 경로 확인은 별도다. 평가 dry-run과 배포 dry-run, 운영 SQL도 통과했다. 독립 최종 리뷰는 **APPROVE**, 열린 지적 0개다 (`.omx/reviews/gemini-migration-code-review.md`). 최종 파일 해시·명령 근거는 `.omx/verification-gemini-migration.json`이다.

Gemini `gemini-3.8-flash`로 전환했다. 평가 CLI의 **4개 합성 사례 × 2전략, 8개 모두 고정 기대를 충족**했다. 실제 호출의 합산 사용량 비용은 **$0.031879**이며 공급자 청구서와 대조한 금액은 아니다.

| 시나리오 | 부분 변경 | 전체 재생성 | 보호 상태 손실 / 불필요한 변경 |
| --- | --- | --- | --- |
| 여행 인원·도착 정정 | 기대 충족 | 기대 충족 | 두 전략 모두 0 / 0 |
| 잠긴 저녁 일정 정정 | 정확한 대상 충돌 1개 | 정확한 대상 충돌 1개 | 두 전략 모두 0 / 0 |
| 과제 마감 연장 | 기대 충족 | 기대 충족 | 두 전략 모두 0 / 0 |
| 바꿔 쓴 정정·자료 속 악성 명령 | 기대 충족 | 기대 충족 | 두 전략 모두 0 / 0 |

부분 변경 비용 합계 $0.013357, 전체 재생성 $0.018522. 지연은 각각 1.995~9.144초와 2.620~6.431초였다. 두 전략 모두 동일한 보호 엔진을 통과했으며 이번 결과에서 의미 품질 우위는 입증되지 않았다. 소규모 고정 사례로 일반 정확도·성능·경쟁 우위를 주장하지 않는다. 안전한 결과 파일은 `PLANS.md`에 연결했다.

실제 로컬 API → Queue → Gemini → D1 확인도 별도로 통과했다. 빈 공간에 합성 여행 원문을 넣어 4개 블록, 4명·고정비 900000·인당 225000을 생성했고, 승인 후 revision 1 저장과 재조회가 성공했다. 제안 준비까지 약 4.096초, 사용량 비용 $0.005504. 테스트 공간 삭제 204/재조회 404로 정리하고 사용자의 기존 공간은 유지했다. `artifacts/gemini-local-service-smoke.json`에 메타데이터만 남겼다.

실제 연결 중 발견한 문제:

- 중첩 배열의 `maxItems`가 포함된 전체 출력 스키마는 400을 반환했다. 최소 스키마 생성은 200, 같은 전체 스키마에서 배열 제한을 제거하면 200이었다. 공급자 요청에서는 해당 제한을 제외하되 Zod의 길이·개수·패턴과 계산/근거/보호 검증은 유지했다. `const`는 공급자 지원 `enum`으로 전달한다.
- CLI 성공 이후 실제 Workers Queue는 `redirect:error`를 받아들이지 못해 TypeError로 실패했다. 실제 Workers Request 파서를 통과하는 회귀 테스트로 재현했고 `manual` + 3xx 거절로 수정했다. 리다이렉트 대상으로 API 키를 전달하지 않는다. 최초 실패는 별도 `gemini-local-service-smoke-before-runtime-fix.json`에 보존했고 예약 비용은 제거하지 않았다.
- 출력 검증 실패와 생성 중 자료 변경으로 폐기된 제안에서도 사용량 기반 비용을 한 번만 장부에 기록하고 로그에 함께 남긴다. 독립 리뷰가 발견한 비용 로그 누락을 두 통합 회귀 사례로 수정했다.
- 가격 유효기간, thinking 포함 사용량, 불명확 비용 유지, 구 모델 예약 차단, 초과 사용량 실제 장부 보존과 전역 차단, CLI 영속 차단을 검증한다. 예약 한도를 청구 강제 상한으로 표현하지 않는다.

운영 SQL은 실제 로컬 D1과 합성 초과 비용/삭제된 실행 장부에서도 통과했다. 예약·실제 비용에 위반 금액을 중복 합산하지 않으며 차단 상태를 표시한다. 증거: `artifacts/ops-status-gemini-validation.json`.

운영 의존성 감사는 알려진 취약점 0건, 전체 개발 의존성은 sharp 전이 경로의 high 4건이 남았다. 호환성 검증 없이 도구를 강제 다운그레이드하지 않았다. 공개 배포, 원격 CI/알림, 처음 보는 자료에 대한 사용자 수동 평가는 별도 확인 항목이다.

## 2026-09-08 API 설정과 평가 실패 진단

11:44 KST root `npm run verify`: **45개 단위 + 41개 D1 통합 = 86개 통과**, 타입·린트·빌드 통과. 평가 dry-run도 통과했다. 이번 변경은 평가 CLI와 오류 분류에 한정하며, Chromium 9개와 배포 dry-run은 아래 오전 검증을 근거로 유지한다. 독립 리뷰는 **APPROVE** (`.omx/reviews/api-setup-code-review.md`). 현재 파일 해시는 `.omx/verification-api-setup.json`에 있다.

- 실제 키를 읽은 개발 서버는 `liveAvailable:true`, 인증된 모델 정보 조회는 HTTP 200을 반환했다. 이는 생성 품질이나 API 잔액을 보장하지 않는다.
- 첫 실제 평가의 8회 호출은 모두 실패했다. 당시 진단은 SDK의 일반 `Error` 이름만 기록하여 정확한 오류 코드를 복원할 수 없다. API 결제 화면의 크레딧 $0.00은 별도로 확인했으며 충전을 사용자에게 인계했다. 결과의 $0.089536은 미확인 사용량에 대한 예약액이며 확정 청구액이 아니다.
- 인증·권한·크레딧·요청 계약·모델 접근·연결 실패를 안전한 코드와 HTTP 상태로 구분한다. 공통 설정 오류와 연결 불확실 오류가 나면 남은 배치를 중단하며 원문 오류 메시지를 보관하지 않는다.
- 새 단위 테스트는 실제 SDK 오류가 `name:Error`여도 정확히 분류하는지 확인한다. 별도 프로세스의 실제 CLI에는 합성 키와 가짜 HTTP 응답만 제공했다. 401과 크레딧 부족 429 모두 8회 계획 중 1회만 요청했고, 하나의 예약액만 유지했으며 키와 오류 본문이 로그·결과에서 제외됐다.
- 새 회귀 테스트의 초기 타입 검사는 생성된 `NodeJS.ProcessEnv` 필수 필드 누락으로 실패했다. 실제 환경 상속 없이 공개 설정값을 명시한 뒤 전체 검증이 통과했다. 추가 유료 모델 평가와 실제 새 원문의 초기 생성 시험은 아직 수행하지 않았다.

## 2026-09-08 오전 잔여 작업 후속 검증

당시 root 검증: **38개 단위 + 41개 D1 통합 + 9개 Chromium = 88개 통과**, 타입·린트·빌드, 평가 dry-run, 배포 dry-run 통과. 독립 코드 리뷰는 **APPROVE**이며 보고서는 `.omx/reviews/release-readiness-code-review.md`에 있다. 아래 야간 03:05 기록은 이전 체크포인트다.

- 브라우저 실행을 5174/8788과 임시 D1으로 분리했다. 부모 환경에 가짜 키와 process-env 포함 설정을 넣어도 테스트 Worker는 키 없이 실행된다. 일반 개발 서버 5173/8787을 함께 켠 상태에서 별도 확인용 작업공간의 내용·버전이 모두 유지됐다.
- Playwright의 기본 강제 종료 대신 명시적인 SIGTERM 유예 종료를 사용한다. 정상 종료와 의도적인 포트 충돌 실패 모두 이번 실행의 서버/임시 디렉터리를 정리했다. 외부 SIGKILL에 대한 정리는 best-effort이며 별도 감시 프로세스는 두지 않는다. 초기 구현 실험의 오래된 디렉터리는 root가 별도로 정리했다.
- 실행 로그는 실제 게시·폐기·무시를 구분하고, 사용량 기반 비용과 예약액 대체 비용을 표시한다. cron은 완료·부분 전송 실패·DB 단계 실패를 구분하며 실제 삭제된 작업공간 수를 기록한다. 회귀 검증에서 원문·목적·키·모델 응답 내용이 로그에 없는지 확인했다.
- `ops:local`의 다섯 SELECT 집계는 실제 D1에서 통과했다. 실제 마이그레이션을 적용한 메모리 DB에서도 대기 시간·삭제 대기·예약/관측 비용 분리·삭제 이후의 미관측 비용을 검증했다. 증거: `artifacts/ops-status-validation.json`.
- 바인딩 타입은 공개된 키 예제 파일로 생성한다. 가짜 키·다른 비밀 변수·상속된 process-env 포함 설정이 있어도 결과 해시가 같았다. CI는 자체 E2E 마이그레이션과 무호출 평가/배포 dry-run을 사용하도록 준비했으며 원격 Actions 실행은 아직 하지 않았다.

현재 파일 해시와 명령 근거는 `.omx/verification-release-readiness.json`, 개발 데이터 격리/정리 근거는 `artifacts/e2e-isolation-validation.json`에 있다. 실제 AI 의미 품질, 원격 D1/Queue/cron, 알림 수신 및 사용자 수동 체험은 여전히 별도 확인 항목이다.

## 야간 구현 체크포인트

상태: 로컬 승인 범위 검증 완료. G003 독립 리뷰의 지적 사항을 G004에서 수정한 뒤 코드 리뷰 **APPROVE**, 아키텍처 리뷰 **CLEAR**를 받았다. 열린 로컬 범위 지적 사항은 없다. 실제 OpenAI 모델 호출과 원격 배포는 수행하지 않았다.

2026-09-08 03:05 KST 최종 수정 후 root 검증: `npm run verify`(타입·린트·38개 단위 테스트·37개 D1 통합 테스트·빌드), `npm run test:e2e`(Chromium 9개), 평가 dry-run, 배포 dry-run 통과. 운영 의존성 감사는 알려진 취약점 0개를 보고했다. 아래의 검증 경계와 함께 해석한다.

## 검증 경계

- Core 단위 테스트는 실제 변경 엔진에 합성 자료와 적대적 제안을 입력한다.
- D1 통합 테스트는 Workers 런타임의 실제 로컬 D1과 트랜잭션을 사용한다. 일부 저장 계층 테스트는 코어를 대역으로 분리하며, 별도 real-core 사례와 브라우저 테스트가 전체 연결을 검증한다.
- Queue 테스트는 실제 handler와 D1 claim을 실행하고 모델 HTTP 응답을 제어한다. 9월 9일 이후 경계는 Gemini다. 실제 유료 모델 품질이나 원격 Queue 전송 품질을 측정한 결과는 아니다.
- 야간 브라우저 검증은 Chromium → Vite 5173 → Worker → D1 경로를 사용했다. 오전 후속부터는 전용 5174/8788 및 임시 D1을 사용한다. 합성 사례는 화면에서 합성임을 표시한다. 실패 복구 테스트의 응답 주입은 따로 구분한다.

## 시나리오와 관측 가능한 기대

| ID | 입력 / 동작 | 확인하는 결과 | 실행 근거 |
| --- | --- | --- | --- |
| C01 | 여행 원문 4명, 고정비 900000 | 인당 225000, 원문 정확 인용 | `tests/unit/engine.test.ts` |
| C02 | 완료 체크·수동 문장·잠금 후 3명/16시 정정 | 인당 300000, 상태·ID 보존, 수동 내용 stale 표시 | core + `tests/e2e/workspace.spec.ts` |
| C03 | 잠금과 충돌하는 저녁 20시 | 명시 선택 전 적용 거절, 선택 후에도 잠금 유지 | core + browser |
| C04 | 추가 자료의 모순, 혼합 keep/use 선택 | 사실·계산 일관성, 보존된 자유 문장 stale 표시 | core |
| C05 | fact-only 제안, 중복 key, 잘못된 참조, 0 나눗셈, 동시 수정/삭제 | 누락 영향 표시 또는 계약 오류, 조용한 데이터 손실 차단 | core |
| C06 | 과제 마감 연장·완료 항목 삭제 제안 | 동일 엔진의 마감 변경, 완료 삭제 보호 | core + browser |
| P01 | 다른 브라우저 owner가 GET/PATCH/DELETE | 모두 404, 원래 내용 동일, HttpOnly cookie | integration + `tests/e2e/security.spec.ts` |
| P02 | 외부/누락/다른 scheme Origin | 쓰기 거절, private 응답 no-store | integration + browser |
| P03 | 중복 원문·동일 요청 재전송 | 원문/실행/적용 중복 방지, 다른 payload 요청 ID 충돌 | integration |
| P04 | 두 요청을 D1 batch 직전 barrier에서 경합 | 한 번만 적용, 실패한 snapshot/command/guard 잔여 없음 | integration 실제 D1 barrier |
| P05 | 남은 마지막 호출 예산을 서로 다른 owner가 경합 | 서비스 전체 일일·누적 상한, 소유자 횟수 제한 | integration 실제 D1 barrier |
| P06 | 모델 실행 전/후 기준 변경, 현재 버전으로 위장한 오래된 제안 | 호출 전 차단 또는 stale 결과 거절, 수동 내용 보존 | integration |
| P07 | 질문이 남은 제안을 직접 API 적용 | UI 우회에도 적용 거절 | core + integration |
| P08 | Queue 중복 전달·전송 실패·예외 | 단일 claim, 저장된 pending 재전달, 불확실 호출 자동 재실행 없음 | integration HTTP fixture |
| P09 | 같은 원본 실행 동시 재시도 | 원본당 명시적 재시도 1개, 별도 비용 예약 | integration |
| P10 | 만료·수동 삭제·만료 후 idempotency replay | raw 내용 제거, 재노출 거절, 비용 장부 유지 | integration + browser |
| P11 | 내용 이력 복원 | 새 내용 버전, 자료 목록·비용·소유권은 과거로 복원하지 않음 | integration + browser |
| P12 | DB에 현재 snapshot 누락 | 빈 성공으로 위장하지 않고 복구 오류 | integration |
| P13 | 6,000자 이모지 자료를 반복 추가 | 전체 24,000자 한도를 같은 UTF-16 단위로 검사 | integration |
| P14 | 중복 자료 기록 중 저장소 장애 | 409 요청 충돌로 숨기지 않고 500 저장소 오류, 기존 자료 유지 | integration 오류 주입 |
| P15 | 만료 직전 선택 조회·다른 조회와 경합 | 선택한 공간만 7일 연장, 만료 부활 없음, 만료 시각 역행 없음 | integration 가상 시계·조회 경합 |
| P16 | 키/예산 없이 재시도·동일 재시도 재전송 | 실패한 사전 조건은 재시도 슬롯 미소모, 첫 응답과 재전송 응답 동일 | integration + browser |
| P17 | 예시 원문 한도·마지막 작업공간 자리 경합 | 예시에도 동일 한도, 정상/예시 동시 생성 중 하나만 성공, 고아 행 없음 | integration 실제 D1 barrier |
| P18 | 이력 목록 조회와 과거 복원 | 목록은 metadata만 조회, 선택한 복원 내용은 별도 조회 | DB 코드 검토 + 기존 복원 integration/browser |
| M01 | 조립 입력 상한 초과·미설정 키·가격 미등록 모델 | 외부 HTTP 전에 차단 | `tests/unit/model.test.ts` |
| M02 | 불완전 응답·거절·누락 usage·연결 끊김 | 계약 오류, 알 수 없는 비용 예약 유지, 자동 retry 0 | model boundary tests |
| E01 | 확인 질문·엉뚱한 충돌·보호 metadata 손실·잘못된 파생값 | 평가에서 성공으로 세지 않음 | `tests/unit/evaluation.test.ts` |
| U01 | 모바일, 새로고침, HTML처럼 보이는 제목 | 가로 넘침 없음, 상태 유지, HTML 실행 없음 | browser |
| U02 | 비정상 JSON 응답, pending 자동 갱신 중 자료 작성 | 조회 실패 후 재시도 가능, 입력 중 문장·관계·대상 보존 | `tests/e2e/recovery.spec.ts` 응답 주입 |

## 발견 후 수정한 실패

- Workerd가 KST 날짜를 아직 미래의 호환 날짜로 거절했다. 지원되는 UTC 날짜로 조정하고 타입을 다시 생성했다.
- Vite와 Worker의 Origin/Host 불일치로 정상 POST가 403이었다. 정확한 개발 Origin과 proxy Host를 함께 맞추고 외부 Origin 거절을 확인했다.
- 모델이 일부 항목을 생략하거나 상충한 사실을 선택할 때 화면 문구와 stale 상태가 사실과 어긋났다. 독립 리뷰 재현을 회귀 테스트로 고쳤다.
- D1의 zero-row CAS를 성공한 트랜잭션처럼 취급할 수 있었다. `tx_guards`와 실패 제약을 통해 전체 batch를 실제로 중단한다.
- 만료 후 cached response replay, 누락 snapshot을 빈 상태로 처리하는 경로, Queue send 실패 후 불명확한 응답을 각각 먼저 실패시키고 수정했다.
- 클라이언트가 제안의 base를 현재 버전 숫자로 바꿔 보내면 오래된 내용이 적용됐다. 서버가 저장된 제안 자체의 base도 현재 상태와 대조한다.
- 브라우저 테스트 cleanup에 Origin이 빠져 403이 발생했다. 정상 작업 흐름의 결과와 구분해 기록하고 정리 요청의 Origin을 수정했다.
- SQLite 문자열 길이와 JavaScript 문자열 길이가 이모지를 다르게 세었다. 전체 원문 한도를 계약과 동일한 UTF-16 단위로 맞췄다.
- 중복 자료의 명령 응답 저장 중 DB 오류가 모두 409로 표시됐다. 실제 unique 제약 실패만 재전송 경로로 처리하고 다른 DB 오류는 서버 오류로 전달한다.
- 독립 리뷰에서 읽기 활동 만료·키 없는 재시도·재시도 응답 차이를 발견했다. 선택 조회만 만료를 연장하고, 재시도 사전 조건과 저장 응답을 바로잡았다. 이어서 동시 조회가 더 늦은 만료를 앞당기지 않도록 `MAX`로 갱신한다.
- 예시 자료의 저장 한도와 작업공간 개수 검사가 일반 경로보다 약했다. 공통 자료 검사와 같은 트랜잭션 안의 개수 guard로 보강했다. 이력 목록은 전체 snapshot payload를 읽지 않는다.

## 남은 증거 경계

고정 합성 사례의 Gemini 해석·지연·사용량과 로컬 Queue 경로는 9월 9일에 별도 검증했다. 처음 보는 자료의 의미 정확도, 원격 D1/Queue/cron, 배포 계정과 운영 알림은 미검증이다. 평가 dry-run은 실행 계획과 입력 상한만 검증한다. 브라우저 검증은 현재 Chromium에 한정되며, 광범위한 장기 부하·다중 지역 장애 시험을 대신하지 않는다.

최종 실행 명령·결과와 독립 리뷰는 `PLANS.md` 및 `.omx/ultragoal/ledger.jsonl`의 마지막 checkpoint를 따른다.

최종 독립 보고서: `.omx/reviews/final-code-review.md`, `.omx/reviews/final-architecture-review.md`. 첫 차수의 지적은 각각 `code-review-round1.md`, `architecture-review-round1.md`에 보존했다. 아키텍처 재검토 담당자는 D1 통합 37개와 브라우저 복구 3개를 별도로 통과시켰다. 구현 불변식과 증거 연결은 `.omx/invariant-audit.md`, 검증 당시 파일 해시는 `.omx/verification-final.json`을 따른다.
