# 문구 수정 배포와 생성 한도 복구 — 2026-09-15

공개 주소는 https://ieojim.jungseongheon.org 이며 Worker 버전은 `a6eba611-ebfb-4063-8f40-76223e74e7c9`다.

## 화면 문구

첫 문장은 “일정 하나 바뀌었다고, 처음부터 다시 짜지 마세요.”로 바꿨다. 설명은 변경된 안내문 → AI의 변경 내용·근거 추출 → 코드의 계산·보호 상태 확인 → 사용자 승인 → 저장 순서다. Gemini 분석, 준비된 체험, 일정 계산, 별도 승인이 필요한 Calendar·메일 처리를 구분한다.

첫 화면, 업무 소개, 작업 생성·편집·검토·준비·이력, 일정 조정, 로그인·계정, 오류와 개인정보·이용 조건을 정리했다. 저장된 원문·직접 작성한 내용·기존 예시 데이터는 덮어쓰지 않았다. 코드 변경은 표시 문구와 그 문구를 확인하는 테스트에 한정한다.

실제 공개 화면 19개 상태를 PC·모바일로 확인한 38개 캡처는 `artifacts/final-copy-2026-09-15/public/`에 있다. 제출용 16:9 화면 5장은 `docs/submission/assets/2026-09-15-final-copy/`에 있다. 예시 데이터로 촬영했으며 실제 AI 호출이나 외부 실행 결과를 뜻하지 않는다.

## 공개 검증 중 발생한 생성 제한

공개 브라우저 검증을 반복하면서 하루 누적 작업 생성이 100건에 도달했다. 작업을 삭제해도 이 누적 기록은 반환되지 않는다. 당시 집계는 활성 작업 4건, 저장 내용 56,258바이트, 당일 명령 107건이었다. 저장 공간 부족이나 AI 오류가 아니라 전역 생성 한도에 따른 429였다. 이 작업의 반복 검증도 한도 소진에 기여했다. 전체 100건이 이 작업에서 생겼다고 단정하지 않는다.

Root는 기존 배포 승인 범위에서 검증으로 막힌 일반 진입을 복구하는 제한된 운영 조치로 판단했다. 사용자가 숫자 변경을 별도로 승인했다고 기록하지 않는다. 독립 리뷰는 다른 제한과 사용 기록을 유지하는 조건에서 100→200 조정을 적절한 복구 방법으로 평가했다.

`storage_policy.max_daily_creations`만 100에서 200으로 조건부 변경했다. 예상한 다른 정책 값이 모두 일치할 때만 한 행을 갱신했고, 별도 재조회로 확인했다. `admission_days`의 생성 100건·명령 107건과 저장량은 그대로였다.

- 활성 작업 최대 200건, 하루 명령 2,000건, 내용 저장량 50MiB는 유지한다.
- IP·소유자 제한, 이력 제한, 만료, AI 비용·호출 제한은 유지한다.
- 새 유료 요금제나 인프라 자원을 추가하지 않았다. 더 많은 생성이 허용되므로 DB 작업량은 늘어날 수 있다.
- **200건 설정은 이후 UTC 날짜에도 유지된다.** 자동으로 100건으로 돌아오는 임시 설정이 아니다.
- 새 DB의 마이그레이션 기본값은 여전히 100건이다. 이번 공개 DB의 운영 설정 차이는 이 문서와 실행 SQL에 남긴다.
- 사용량 비율 경보는 새 200건 기준이 된다. 같은 100건에서도 경보 민감도가 달라진다는 점을 운영 시 고려한다.

실행 SQL과 전후 집계는 `.omx/final-copy-2026-09-15/restore-creation-capacity.sql`, `admission-confirmed.json`, `admission-after-policy.json`에 보관한다. 실패한 공개 실행 보고서도 보존한다. 한도 때문에 실패한 9개만 다시 실행하며, 이미 같은 코드에서 통과한 18개를 불필요하게 재실행하지 않는다.

## 다음 배포 시 검증 예산

로컬 전체 검증을 먼저 끝낸다. 공개 환경에서 생성이 필요한 검증 전에는 `scripts/ops-status.sql`로 남은 생성·명령·저장·AI 용량을 확인한다. 테스트와 촬영에 필요한 작업 수를 먼저 계산하고 일반 사용자 몫을 남긴다. 이미 확인한 공개 경로를 단지 깨끗한 단일 보고서를 얻기 위해 반복하지 않는다. 검증용 작업만 지우며, 사용 기록을 지워 통과시키지 않는다.

100건으로 되돌리려면 먼저 현재 정책과 당일 사용량을 확인한다. 당일 생성이 이미 100건 이상이면 즉시 신규 생성이 다시 막힌다. 원복도 현재 200건일 때만 조건부로 실행하고 전후 집계를 남긴다. 장부를 리셋하지 않는다.

최종 검사 결과와 남은 사항은 `.omx/verification-final-copy-2026-09-15.json` 및 `PLANS.md`의 최신 재개 항목에 기록한다. 대회 사이트 제출은 이번 작업에서 수행하지 않았다.
